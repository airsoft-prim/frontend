# TODO

Задачи, которые отложены сознательно: место зафиксировано, чтобы к работе можно
было вернуться с тем же контекстом.

## Переработать ленту событий (`/feed/`)

Страница собрана как каркас: разметка и поведение на месте, данных нет.

- подключить API событий: список, реальный счётчик страниц, наполнение строк
  (превью, заголовок, тип, дата публикации, автор) вместо скелетонов;
- вкладки «Все / Игры / Объявления» сейчас только переключают состояние
  и не фильтруют выборку — связать их с запросом;
- `total={8}` в селекторе страниц — заглушка, проверить листание на живом
  объёме;
- решить, нужна ли странице левая колонка (`SiteAside`): лента из одних строк
  может выиграть от полной ширины;
- переименовать плашку в сайдабаре: сейчас «События и объявления» и
  «Все объявления», по смыслу — «Новости и события» и «Все события»
  (в этой итерации решено оставить как есть).

## Переработать правила страйкбола (`/rules/`)

Текст — первый черновик, написанный под каркас страницы: формулировки и цифры
с сообществом не согласованы.

- вычитать документ с организаторами: ограничения по классам приводов,
  дистанции ближнего боя, возврат в игру и медики, пиротехника, допуск
  несовершеннолетних;
- развести два документа: `/rules/` — правила игры, `/agreement/` — правила
  портала; сейчас они ссылаются друг на друга, но границу стоит подтвердить;
- добавить оглавление с якорями: разделов двенадцать, по документу сложно
  ориентироваться;
- поставить ссылку в подвале (`SiteFooter`, `PAGE_LINKS`) рядом с «Правилами
  использования» и «Политикой конфиденциальности»;
- решить, показывать ли версию и дату документа в шапке страницы, а не только
  строкой в конце текста.
