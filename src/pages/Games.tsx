import { SiteLayout } from '../components/SiteLayout';

/**
 * Раздел «Игры». Контент убран намеренно: страница держит пустое
 * пространство, пока не появится настоящая лента игр.
 */
export function Games() {
  return <SiteLayout />;
}
