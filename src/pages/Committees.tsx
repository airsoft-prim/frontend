import { SiteLayout } from '../components/SiteLayout';

/**
 * Раздел «Орг. комитеты». Заглушка: страница существует, чтобы вкладка
 * в шапке не вела в 404. Контент появится вместе с разделом.
 */
export function Committees() {
  return <SiteLayout />;
}
